---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { useTranslations } from '../../i18n/ui';

const locale = 'fr';
const t = useTranslations(locale);
---

<BaseLayout
  title="Inscription - CIRIV 2026"
  description="Inscrivez-vous à la Conférence Internationale One Health CIRIV 2026"
  locale={locale}
>
  <Header locale={locale} currentPath={Astro.url.pathname} />
  
  <main class="min-h-screen bg-gradient-to-br from-slate-50 to-teal-50 dark:from-surface-dark dark:to-slate-800 py-12">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto">
        <!-- Header -->
        <div class="text-center mb-12">
          <h1 class="text-4xl font-bold text-slate-900 dark:text-white mb-4">
            Inscription CIRIV 2026
          </h1>
          <p class="text-xl text-slate-600 dark:text-slate-300">
            Rejoignez la première conférence internationale One Health au Maroc
          </p>
        </div>

        <!-- Progress Bar -->
        <div class="mb-8">
          <div class="flex items-center justify-center space-x-4">
            <div class="flex items-center">
              <div id="step1-indicator" class="w-8 h-8 bg-brand-primary text-white rounded-full flex items-center justify-center font-semibold">
                1
              </div>
              <span class="ml-2 text-sm font-medium text-slate-700 dark:text-slate-300">Informations personnelles</span>
            </div>
            <div class="w-16 h-0.5 bg-slate-300 dark:bg-slate-600"></div>
            <div class="flex items-center">
              <div id="step2-indicator" class="w-8 h-8 bg-slate-300 dark:bg-slate-600 text-slate-500 rounded-full flex items-center justify-center font-semibold">
                2
              </div>
              <span class="ml-2 text-sm font-medium text-slate-500">Type de participation</span>
            </div>
          </div>
        </div>

        <!-- Registration Form -->
        <div class="bg-white dark:bg-slate-800 rounded-2xl shadow-xl p-8">
          <form id="registration-form" class="space-y-6">
            
            <!-- Step 1: Personal Information -->
            <div id="step1" class="space-y-6">
              <h2 class="text-2xl font-semibold text-slate-900 dark:text-white mb-6">
                Étape 1: Informations personnelles
              </h2>

              <div class="grid md:grid-cols-2 gap-6">
                <!-- Nom -->
                <div>
                  <label for="nom" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    Nom <span class="text-red-500">*</span>
                  </label>
                  <input
                    type="text"
                    id="nom"
                    name="nom"
                    required
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                    placeholder="Votre nom de famille"
                  />
                </div>

                <!-- Prénom -->
                <div>
                  <label for="prenom" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    Prénom <span class="text-red-500">*</span>
                  </label>
                  <input
                    type="text"
                    id="prenom"
                    name="prenom"
                    required
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                    placeholder="Votre prénom"
                  />
                </div>
              </div>

              <div class="grid md:grid-cols-2 gap-6">
                <!-- Civilité -->
                <div>
                  <label for="civilite" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    Civilité / Sexe
                  </label>
                  <select
                    id="civilite"
                    name="civilite"
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                  >
                    <option value="">Sélectionner</option>
                    <option value="M.">M.</option>
                    <option value="Mme">Mme</option>
                  </select>
                </div>

                <!-- Email -->
                <div>
                  <label for="email" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    Email <span class="text-red-500">*</span>
                  </label>
                  <input
                    type="email"
                    id="email"
                    name="email"
                    required
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                    placeholder="votre@email.com"
                  />
                </div>
              </div>

              <div class="grid md:grid-cols-2 gap-6">
                <!-- Téléphone -->
                <div>
                  <label for="telephone" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    Téléphone
                  </label>
                  <input
                    type="tel"
                    id="telephone"
                    name="telephone"
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                    placeholder="+212 6XX-XXXXXX"
                  />
                </div>

                <!-- Établissement -->
                <div>
                  <label for="etablissement" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    Établissement <span class="text-red-500">*</span>
                  </label>
                  <input
                    type="text"
                    id="etablissement"
                    name="etablissement"
                    required
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                    placeholder="Nom de votre établissement"
                  />
                </div>
              </div>

              <div class="grid md:grid-cols-2 gap-6">
                <!-- Laboratoire / Département -->
                <div>
                  <label for="departement" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    Laboratoire / Département
                  </label>
                  <input
                    type="text"
                    id="departement"
                    name="departement"
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                    placeholder="Laboratoire ou département"
                  />
                </div>

                <!-- Ville & Pays -->
                <div>
                  <label for="ville" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    Ville & Pays <span class="text-red-500">*</span>
                  </label>
                  <input
                    type="text"
                    id="ville"
                    name="ville"
                    required
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                    placeholder="Ville, Pays"
                  />
                </div>
              </div>

              <!-- Statut -->
              <div>
                <label for="statut" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                  Statut <span class="text-red-500">*</span>
                </label>
                <select
                  id="statut"
                  name="statut"
                  required
                  class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                >
                  <option value="">Sélectionnez votre statut</option>
                  <option value="Étudiant">Étudiant</option>
                  <option value="Doctorant">Doctorant</option>
                  <option value="Enseignant-chercheur">Enseignant-chercheur</option>
                  <option value="Chercheur">Chercheur</option>
                  <option value="Professionnel">Professionnel</option>
                </select>
              </div>

              <!-- Next Button -->
              <div class="flex justify-end">
                <button
                  type="button"
                  id="next-step"
                  class="px-8 py-3 bg-brand-primary hover:bg-brand-primaryDark text-white font-semibold rounded-lg transition-all duration-300 hover:scale-105 hover:shadow-lg focus:outline-none focus:ring-2 focus:ring-brand-accent focus:ring-offset-2 dark:focus:ring-offset-slate-800"
                >
                  Suivant →
                </button>
              </div>
            </div>

            <!-- Step 2: Participation Type -->
            <div id="step2" class="space-y-6 hidden">
              <h2 class="text-2xl font-semibold text-slate-900 dark:text-white mb-6">
                Étape 2: Type de participation
              </h2>

              <!-- Participation Type -->
              <div>
                <label class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-4">
                  Comment souhaitez-vous participer ? <span class="text-red-500">*</span>
                </label>
                <div class="space-y-3">
                  <label class="flex items-center p-4 border border-slate-300 dark:border-slate-600 rounded-lg hover:bg-slate-50 dark:hover:bg-slate-700 cursor-pointer transition-colors">
                    <input
                      type="radio"
                      name="typeParticipation"
                      value="Participant"
                      class="w-4 h-4 text-brand-primary focus:ring-brand-accent border-slate-300 dark:border-slate-600"
                    />
                    <div class="ml-3">
                      <div class="text-sm font-medium text-slate-900 dark:text-white">Participant</div>
                      <div class="text-sm text-slate-500 dark:text-slate-400">Assister aux conférences et participer aux discussions</div>
                    </div>
                  </label>
                  <label class="flex items-center p-4 border border-slate-300 dark:border-slate-600 rounded-lg hover:bg-slate-50 dark:hover:bg-slate-700 cursor-pointer transition-colors">
                    <input
                      type="radio"
                      name="typeParticipation"
                      value="Présentateur"
                      class="w-4 h-4 text-brand-primary focus:ring-brand-accent border-slate-300 dark:border-slate-600"
                    />
                    <div class="ml-3">
                      <div class="text-sm font-medium text-slate-900 dark:text-white">Présentateur</div>
                      <div class="text-sm text-slate-500 dark:text-slate-400">Présenter une communication (orale ou poster)</div>
                    </div>
                  </label>
                </div>
              </div>

              <!-- Presenter Fields (Hidden by default) -->
              <div id="presenter-fields" class="space-y-6 hidden">
                <div class="border-t border-slate-200 dark:border-slate-700 pt-6">
                  <h3 class="text-lg font-medium text-slate-900 dark:text-white mb-4">
                    Informations sur votre communication
                  </h3>

                  <!-- Titre de la communication -->
                  <div class="mb-4">
                    <label for="titreCommunication" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                      Titre de la communication <span class="text-red-500">*</span>
                    </label>
                    <input
                      type="text"
                      id="titreCommunication"
                      name="titreCommunication"
                      class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                      placeholder="Titre de votre communication"
                    />
                  </div>

                  <div class="grid md:grid-cols-2 gap-6">
                    <!-- Auteurs -->
                    <div>
                      <label for="auteurs" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                        Auteurs <span class="text-red-500">*</span>
                      </label>
                      <input
                        type="text"
                        id="auteurs"
                        name="auteurs"
                        class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                        placeholder="Nom1, Nom2, Nom3..."
                      />
                    </div>

                    <!-- Auteur correspondant -->
                    <div>
                      <label for="auteurCorrespondant" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                        Auteur correspondant <span class="text-red-500">*</span>
                      </label>
                      <input
                        type="text"
                        id="auteurCorrespondant"
                        name="auteurCorrespondant"
                        class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                        placeholder="Nom et email de l'auteur correspondant"
                      />
                    </div>
                  </div>

                  <div class="grid md:grid-cols-2 gap-6">
                    <!-- Thématique -->
                    <div>
                      <label for="thematique" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                        Thématique <span class="text-red-500">*</span>
                      </label>
                      <select
                        id="thematique"
                        name="thematique"
                        class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                      >
                        <option value="">Sélectionnez une thématique</option>
                        <option value="Santé humaine">Santé humaine</option>
                        <option value="Santé animale">Santé animale</option>
                        <option value="Environnement">Environnement</option>
                        <option value="Interdisciplinaire">Interdisciplinaire</option>
                      </select>
                    </div>

                    <!-- Choix de communication -->
                    <div>
                      <label for="choixCommunication" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                        Choix de communication <span class="text-red-500">*</span>
                      </label>
                      <select
                        id="choixCommunication"
                        name="choixCommunication"
                        class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                      >
                        <option value="">Sélectionnez le type</option>
                        <option value="Communication orale">Communication orale</option>
                        <option value="Communication affichée / poster">Communication affichée / poster</option>
                      </select>
                    </div>
                  </div>

                  <!-- Résumé File Upload -->
                  <div>
                    <label for="resumeFile" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                      Résumé (fichier) <span class="text-red-500">*</span>
                    </label>
                    <div class="relative">
                      <input
                        type="file"
                        id="resumeFile"
                        name="resumeFile"
                        accept=".pdf,.doc,.docx"
                        class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-semibold file:bg-brand-primary file:text-white hover:file:bg-brand-primaryDark"
                      />
                    </div>
                    <p class="mt-2 text-sm text-slate-500 dark:text-slate-400">
                      Formats acceptés: PDF, DOC, DOCX (max 5MB)
                    </p>
                  </div>
                </div>
              </div>

              <!-- Navigation Buttons -->
              <div class="flex justify-between pt-6">
                <button
                  type="button"
                  id="prev-step"
                  class="px-8 py-3 border border-slate-300 dark:border-slate-600 text-slate-700 dark:text-slate-300 font-semibold rounded-lg hover:bg-slate-50 dark:hover:bg-slate-700 transition-colors"
                >
                  ← Précédent
                </button>
                <button
                  type="submit"
                  id="submit-form"
                  class="px-8 py-3 bg-green-600 hover:bg-green-700 text-white font-semibold rounded-lg transition-all duration-300 hover:scale-105 hover:shadow-lg focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 dark:focus:ring-offset-slate-800"
                >
                  Envoyer l'inscription
                </button>
              </div>
            </div>
          </form>

          <!-- Status Messages with Enhanced Animation -->
          <div id="status-message" class="hidden mb-6">
            <div id="status-content" class="p-6 rounded-lg text-center font-medium transform transition-all duration-500 ease-in-out">
              <div id="status-icon" class="mb-4 flex justify-center">
                <!-- Icons will be dynamically inserted here -->
              </div>
              <div id="status-text" class="text-lg">
                <!-- Message text will be inserted here -->
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
  
  <Footer locale={locale} />
</BaseLayout>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Enhanced DOM element selection with type safety
  const form = document.getElementById('registration-form');
  const step1 = document.getElementById('step1');
  const step2 = document.getElementById('step2');
  const nextBtn = document.getElementById('next-step');
  const prevBtn = document.getElementById('prev-step');
  const step1Indicator = document.getElementById('step1-indicator');
  const step2Indicator = document.getElementById('step2-indicator');
  const participationRadios = document.querySelectorAll('input[name="typeParticipation"]');
  const presenterFields = document.getElementById('presenter-fields');
  const statusMessage = document.getElementById('status-message');

  // Google Apps Script endpoint
  const SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbxUh2TEJ66SnlhsuQhpUL8bD5UQNy5_8KzNX4VfYWzAn8pVyRAxjlSJVSFYjvoU4QbDNw/exec';

  // Enhanced message display with professional animations
  function showMessage(type, title, description) {
    if (!statusMessage) return;

    const statusIcon = statusMessage.querySelector('.status-icon');
    const statusTitle = statusMessage.querySelector('.status-title');
    const statusDesc = statusMessage.querySelector('.status-description');
    const statusContent = statusMessage.querySelector('.status-content');

    if (!statusContent) return;

    // Reset and apply base classes
    statusContent.className = 'status-content p-6 rounded-lg text-center font-medium transform transition-all duration-500 ease-in-out opacity-0 translate-y-4';
    
    // Apply type-specific styling and animations
    switch (type) {
      case 'loading':
        statusContent.classList.add('bg-blue-500', 'text-white');
        if (statusIcon) {
          statusIcon.innerHTML = '⏳';
          statusIcon.classList.add('animate-spin');
        }
        break;
      case 'success':
        statusContent.classList.add('bg-green-500', 'text-white');
        if (statusIcon) {
          statusIcon.innerHTML = '✅';
          statusIcon.classList.add('animate-bounce');
        }
        // Hide form on success
        if (form) form.style.display = 'none';
        break;
      case 'error':
        statusContent.classList.add('bg-red-500', 'text-white');
        if (statusIcon) {
          statusIcon.innerHTML = '❌';
          statusIcon.classList.add('animate-pulse');
        }
        break;
    }

    // Set content
    if (statusTitle) statusTitle.textContent = title;
    if (statusDesc) {
      if (description) {
        statusDesc.textContent = description;
        statusDesc.style.display = 'block';
      } else {
        statusDesc.style.display = 'none';
      }
    }

    // Show message with smooth animation
    statusMessage.classList.remove('hidden');
    
    // Trigger animation after a small delay
    setTimeout(() => {
      statusContent.classList.remove('opacity-0', 'translate-y-4');
      statusContent.classList.add('opacity-100', 'translate-y-0', 'scale-105');
    }, 100);

    // Auto-hide success/error messages after 8 seconds
    if (type !== 'loading') {
      setTimeout(() => {
        hideMessage();
      }, 8000);
    }
  }

  function hideMessage() {
    if (!statusMessage) return;
    
    const statusContent = statusMessage.querySelector('.status-content');
    if (statusContent) {
      statusContent.classList.remove('opacity-100', 'translate-y-0', 'scale-105');
      statusContent.classList.add('opacity-0', 'translate-y-4');
    }
    
    setTimeout(() => {
      statusMessage.classList.add('hidden');
      // Show form again if it was hidden
      if (form && form.style.display === 'none') {
        form.style.display = 'block';
      }
    }, 500);
  }

  // Step navigation with enhanced animations
  function showStep1() {
    if (!step1 || !step2 || !step1Indicator || !step2Indicator) return;
    
    step1.classList.remove('hidden');
    step2.classList.add('hidden');
    step1Indicator.classList.remove('bg-slate-300', 'dark:bg-slate-600', 'text-slate-500');
    step1Indicator.classList.add('bg-brand-primary', 'text-white');
    step2Indicator.classList.remove('bg-brand-primary', 'text-white');
    step2Indicator.classList.add('bg-slate-300', 'dark:bg-slate-600', 'text-slate-500');
  }

  function showStep2() {
    if (!step1 || !step2 || !step1Indicator || !step2Indicator) return;
    
    step1.classList.add('hidden');
    step2.classList.remove('hidden');
    step2Indicator.classList.remove('bg-slate-300', 'dark:bg-slate-600', 'text-slate-500');
    step2Indicator.classList.add('bg-brand-primary', 'text-white');
  }

  // Enhanced validation with better error messages
  function validateStep1() {
    if (!step1) return false;
    
    const requiredFields = step1.querySelectorAll('input[required], select[required]');
    let isValid = true;

    requiredFields.forEach(field => {
      if (!field.value.trim()) {
        field.classList.add('border-red-500', 'shake');
        isValid = false;
        // Remove shake animation after it completes
        setTimeout(() => field.classList.remove('shake'), 500);
      } else {
        field.classList.remove('border-red-500');
      }
    });

    if (!isValid) {
      showMessage('error', '❌ Champs requis manquants', 'Veuillez remplir tous les champs obligatoires pour continuer.');
      return false;
    }

    return isValid;
  }

  function validateStep2() {
    const participationType = document.querySelector('input[name="typeParticipation"]:checked');
    
    if (!participationType) {
      showMessage('error', '❌ Type de participation requis', 'Veuillez sélectionner votre type de participation.');
      return false;
    }

    if (participationType.value === 'Présentateur' && presenterFields) {
      const presenterRequiredFields = presenterFields.querySelectorAll('input[required], select[required]');
      let isValid = true;

      presenterRequiredFields.forEach(field => {
        if (!field.value.trim()) {
          field.classList.add('border-red-500', 'shake');
          isValid = false;
          setTimeout(() => field.classList.remove('shake'), 500);
        } else {
          field.classList.remove('border-red-500');
        }
      });

      if (!isValid) {
        showMessage('error', '❌ Informations de présentation requises', 'Veuillez remplir tous les champs obligatoires pour les présentateurs.');
        return false;
      }
    }

    return true;
  }

  // Enhanced form data collection
  function collectFormData() {
    if (!form) return {};
    
    const formData = new FormData(form);
    const data = {};

    // Collect basic form data
    for (let [key, value] of formData.entries()) {
      if (key !== 'resumeFile') {
        data[key] = value;
      }
    }

    // Handle file upload
    const fileInput = document.getElementById('resumeFile');
    if (fileInput && fileInput.files[0]) {
      data.resumeFileURL = fileInput.files[0].name;
    } else {
      data.resumeFileURL = '';
    }

    // Set default values for optional fields
    data.civilite = data.civilite || '';
    data.telephone = data.telephone || '';
    data.departement = data.departement || '';
    data.titreCommunication = data.titreCommunication || '';
    data.auteurs = data.auteurs || '';
    data.auteurCorrespondant = data.auteurCorrespondant || '';
    data.thematique = data.thematique || '';
    data.choixCommunication = data.choixCommunication || '';

    return data;
  }

  // Event listeners
  if (nextBtn) {
    nextBtn.addEventListener('click', function() {
      if (validateStep1()) {
        showStep2();
      }
    });
  }

  if (prevBtn) {
    prevBtn.addEventListener('click', function() {
      showStep1();
    });
  }

  // Participation type change with smooth transitions
  participationRadios.forEach(radio => {
    radio.addEventListener('change', function() {
      if (!presenterFields) return;
      
      if (this.value === 'Présentateur') {
        presenterFields.classList.remove('hidden');
        presenterFields.style.opacity = '0';
        presenterFields.style.transform = 'translateY(20px)';
        
        // Animate in
        setTimeout(() => {
          presenterFields.style.transition = 'all 0.3s ease-in-out';
          presenterFields.style.opacity = '1';
          presenterFields.style.transform = 'translateY(0)';
        }, 50);
        
        // Make presenter fields required
        const presenterInputs = presenterFields.querySelectorAll('input[required], select[required]');
        presenterInputs.forEach(input => input.setAttribute('required', 'required'));
      } else {
        // Animate out
        presenterFields.style.transition = 'all 0.3s ease-in-out';
        presenterFields.style.opacity = '0';
        presenterFields.style.transform = 'translateY(-20px)';
        
        setTimeout(() => {
          presenterFields.classList.add('hidden');
        }, 300);
        
        // Remove required attribute from presenter fields
        const presenterInputs = presenterFields.querySelectorAll('input, select');
        presenterInputs.forEach(input => input.removeAttribute('required'));
      }
    });
  });

  // Enhanced form submission with comprehensive error handling
  if (form) {
    form.addEventListener('submit', async function(e) {
      e.preventDefault();
      
      if (!validateStep2()) {
        return;
      }

      // Show loading with enhanced animation
      showMessage('loading', '⏳ Envoi en cours...', 'Veuillez patienter pendant que nous traitons votre inscription. Cela peut prendre quelques instants.');

      try {
        const formData = collectFormData();
        console.log('📤 Sending registration data:', formData);
        
        const response = await fetch(SCRIPT_URL, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'Accept': 'application/json'
          },
          body: JSON.stringify(formData),
          mode: 'cors'
        });

        console.log('📡 Response status:', response.status);
        
        if (!response.ok) {
          throw new Error(`Erreur HTTP: ${response.status} - ${response.statusText}`);
        }
        
        const result = await response.json();
        console.log('📥 Server response:', result);

        // Check for various success indicators
        if (result.result === 'success' || result.status === 'success' || result.success === true) {
          showMessage('success', '✅ Inscription réussie !', 'Votre inscription a été enregistrée avec succès. Vous recevrez une confirmation par email sous peu.');
        } else {
          throw new Error(result.error || result.message || 'Réponse inattendue du serveur');
        }
        
      } catch (error) {
        console.error('❌ Submission error:', error);
        
        let errorMessage = 'Une erreur technique est survenue. Veuillez vérifier votre connexion Internet et réessayer.';
        
        if (error.message.includes('fetch')) {
          errorMessage = 'Impossible de contacter le serveur. Vérifiez votre connexion Internet.';
        } else if (error.message.includes('JSON')) {
          errorMessage = 'Erreur de format de réponse du serveur. Veuillez réessayer.';
        } else if (error.message.includes('HTTP')) {
          errorMessage = 'Erreur du serveur. Veuillez réessayer dans quelques instants.';
        }
        
        showMessage('error', '❌ Erreur d\'envoi', errorMessage);
      }
    });
  }

  // Initialize form
  showStep1();
});
</script>

<style>
  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    25% { transform: translateX(-5px); }
    75% { transform: translateX(5px); }
  }
  
  .shake {
    animation: shake 0.5s ease-in-out;
  }
  
  .status-content {
    will-change: transform, opacity;
  }
  
  /* Enhanced loading animation */
  @keyframes pulse-glow {
    0%, 100% {
      box-shadow: 0 0 20px rgba(59, 130, 246, 0.5);
    }
    50% {
      box-shadow: 0 0 40px rgba(59, 130, 246, 0.8);
    }
  }
  
  .status-content.bg-blue-500 {
    animation: pulse-glow 2s infinite;
  }
</style>