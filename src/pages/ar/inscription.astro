---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { useTranslations } from '../../i18n/ui';

const locale = 'ar';
const t = useTranslations(locale);
---

<BaseLayout
  title="التسجيل - CIRIV 2026"
  description="سجل في المؤتمر الدولي للبحث والابتكار والتثمين CIRIV 2026"
  locale={locale}
>
  <Header locale={locale} currentPath={Astro.url.pathname} />
  
  <main class="min-h-screen bg-gradient-to-br from-slate-50 to-teal-50 dark:from-surface-dark dark:to-slate-800 py-12" dir="rtl">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto">
        <!-- Header -->
        <div class="text-center mb-12">
          <h1 class="text-4xl font-bold text-slate-900 dark:text-white mb-4">
            التسجيل في CIRIV 2026
          </h1>
          <p class="text-xl text-slate-600 dark:text-slate-300">
            انضم إلى أول مؤتمر دولي للبحث والابتكار والتثمين في المغرب
          </p>
        </div>

        <!-- Progress Bar -->
        <div class="mb-8">
          <div class="flex items-center justify-center space-x-4 space-x-reverse">
            <div class="flex items-center">
              <div id="step1-indicator" class="w-8 h-8 bg-brand-primary text-white rounded-full flex items-center justify-center font-semibold">
                1
              </div>
              <span class="mr-2 text-sm font-medium text-slate-700 dark:text-slate-300">المعلومات الشخصية</span>
            </div>
            <div class="w-16 h-0.5 bg-slate-300 dark:bg-slate-600"></div>
            <div class="flex items-center">
              <div id="step2-indicator" class="w-8 h-8 bg-slate-300 dark:bg-slate-600 text-slate-500 rounded-full flex items-center justify-center font-semibold">
                2
              </div>
              <span class="mr-2 text-sm font-medium text-slate-500">نوع المشاركة</span>
            </div>
          </div>
        </div>

        <!-- Registration Form -->
        <div class="bg-white dark:bg-slate-800 rounded-2xl shadow-xl p-8">
          <form id="registration-form" class="space-y-6">
            
            <!-- Step 1: Personal Information -->
            <div id="step1" class="space-y-6">
              <h2 class="text-2xl font-semibold text-slate-900 dark:text-white mb-6">
                الخطوة 1: المعلومات الشخصية
              </h2>

              <div class="grid md:grid-cols-2 gap-6">
                <!-- اللقب -->
                <div>
                  <label for="nom" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    اللقب <span class="text-red-500">*</span>
                  </label>
                  <input
                    type="text"
                    id="nom"
                    name="nom"
                    required
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                    placeholder="لقبك العائلي"
                  />
                </div>

                <!-- الاسم -->
                <div>
                  <label for="prenom" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    الاسم <span class="text-red-500">*</span>
                  </label>
                  <input
                    type="text"
                    id="prenom"
                    name="prenom"
                    required
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                    placeholder="اسمك الشخصي"
                  />
                </div>
              </div>

              <div class="grid md:grid-cols-2 gap-6">
                <!-- اللقب / الجنس -->
                <div>
                  <label for="civilite" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    اللقب / الجنس
                  </label>
                  <select
                    id="civilite"
                    name="civilite"
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                  >
                    <option value="">اختر</option>
                    <option value="السيد">السيد</option>
                    <option value="السيدة">السيدة</option>
                  </select>
                </div>

                <!-- البريد الإلكتروني -->
                <div>
                  <label for="email" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    البريد الإلكتروني <span class="text-red-500">*</span>
                  </label>
                  <input
                    type="email"
                    id="email"
                    name="email"
                    required
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                    placeholder="your@email.com"
                  />
                </div>
              </div>

              <div class="grid md:grid-cols-2 gap-6">
                <!-- الهاتف -->
                <div>
                  <label for="telephone" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    الهاتف
                  </label>
                  <input
                    type="tel"
                    id="telephone"
                    name="telephone"
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                    placeholder="+212 6XX-XXXXXX"
                  />
                </div>

                <!-- المؤسسة -->
                <div>
                  <label for="etablissement" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    المؤسسة <span class="text-red-500">*</span>
                  </label>
                  <input
                    type="text"
                    id="etablissement"
                    name="etablissement"
                    required
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                    placeholder="اسم مؤسستك"
                  />
                </div>
              </div>

              <div class="grid md:grid-cols-2 gap-6">
                <!-- المختبر / القسم -->
                <div>
                  <label for="departement" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    المختبر / القسم
                  </label>
                  <input
                    type="text"
                    id="departement"
                    name="departement"
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                    placeholder="المختبر أو القسم"
                  />
                </div>

                <!-- المدينة والبلد -->
                <div>
                  <label for="ville" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                    المدينة والبلد <span class="text-red-500">*</span>
                  </label>
                  <input
                    type="text"
                    id="ville"
                    name="ville"
                    required
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                    placeholder="المدينة، البلد"
                  />
                </div>
              </div>

              <!-- الصفة -->
              <div>
                <label for="statut" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                  الصفة <span class="text-red-500">*</span>
                </label>
                <select
                  id="statut"
                  name="statut"
                  required
                  class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                >
                  <option value="">اختر صفتك</option>
                  <option value="طالب">طالب</option>
                  <option value="طالب دكتوراه">طالب دكتوراه</option>
                  <option value="أستاذ-باحث">أستاذ-باحث</option>
                  <option value="باحث">باحث</option>
                  <option value="مهني">مهني</option>
                </select>
              </div>

              <!-- زر التالي -->
              <div class="flex justify-start">
                <button
                  type="button"
                  id="next-step"
                  class="px-8 py-3 bg-brand-primary hover:bg-brand-primaryDark text-white font-semibold rounded-lg transition-all duration-300 hover:scale-105 hover:shadow-lg focus:outline-none focus:ring-2 focus:ring-brand-accent focus:ring-offset-2 dark:focus:ring-offset-slate-800"
                >
                  ← التالي
                </button>
              </div>
            </div>

            <!-- Step 2: Participation Type -->
            <div id="step2" class="space-y-6 hidden">
              <h2 class="text-2xl font-semibold text-slate-900 dark:text-white mb-6">
                الخطوة 2: نوع المشاركة
              </h2>

              <!-- نوع المشاركة -->
              <div>
                <label class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-4">
                  كيف تريد المشاركة؟ <span class="text-red-500">*</span>
                </label>
                <div class="space-y-3">
                  <label class="flex items-center p-4 border border-slate-300 dark:border-slate-600 rounded-lg hover:bg-slate-50 dark:hover:bg-slate-700 cursor-pointer transition-colors">
                    <input
                      type="radio"
                      name="typeParticipation"
                      value="مشارك"
                      class="w-4 h-4 text-brand-primary focus:ring-brand-accent border-slate-300 dark:border-slate-600"
                    />
                    <div class="mr-3">
                      <div class="text-sm font-medium text-slate-900 dark:text-white">مشارك</div>
                      <div class="text-sm text-slate-500 dark:text-slate-400">حضور المؤتمرات والمشاركة في المناقشات</div>
                    </div>
                  </label>
                  <label class="flex items-center p-4 border border-slate-300 dark:border-slate-600 rounded-lg hover:bg-slate-50 dark:hover:bg-slate-700 cursor-pointer transition-colors">
                    <input
                      type="radio"
                      name="typeParticipation"
                      value="مقدم عرض"
                      class="w-4 h-4 text-brand-primary focus:ring-brand-accent border-slate-300 dark:border-slate-600"
                    />
                    <div class="mr-3">
                      <div class="text-sm font-medium text-slate-900 dark:text-white">مقدم عرض</div>
                      <div class="text-sm text-slate-500 dark:text-slate-400">تقديم بحث (شفهي أو ملصق)</div>
                    </div>
                  </label>
                </div>
              </div>

              <!-- حقول المقدم (مخفية افتراضياً) -->
              <div id="presenter-fields" class="space-y-6 hidden">
                <div class="border-t border-slate-200 dark:border-slate-700 pt-6">
                  <h3 class="text-lg font-medium text-slate-900 dark:text-white mb-4">
                    معلومات حول عرضك
                  </h3>

                  <!-- عنوان البحث -->
                  <div class="mb-4">
                    <label for="titreCommunication" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                      عنوان البحث <span class="text-red-500">*</span>
                    </label>
                    <input
                      type="text"
                      id="titreCommunication"
                      name="titreCommunication"
                      class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                      placeholder="عنوان بحثك"
                    />
                  </div>

                  <div class="grid md:grid-cols-2 gap-6">
                    <!-- المؤلفون -->
                    <div>
                      <label for="auteurs" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                        المؤلفون <span class="text-red-500">*</span>
                      </label>
                      <input
                        type="text"
                        id="auteurs"
                        name="auteurs"
                        class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                        placeholder="اسم1، اسم2، اسم3..."
                      />
                    </div>

                    <!-- المؤلف المراسل -->
                    <div>
                      <label for="auteurCorrespondant" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                        المؤلف المراسل <span class="text-red-500">*</span>
                      </label>
                      <input
                        type="text"
                        id="auteurCorrespondant"
                        name="auteurCorrespondant"
                        class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                        placeholder="اسم وبريد المؤلف المراسل"
                      />
                    </div>
                  </div>

                  <div class="grid md:grid-cols-2 gap-6">
                    <!-- الموضوع -->
                    <div>
                      <label for="thematique" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                        الموضوع <span class="text-red-500">*</span>
                      </label>
                      <select
                        id="thematique"
                        name="thematique"
                        class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                      >
                        <option value="">اختر موضوعاً</option>
                        <option value="الصحة البشرية">الصحة البشرية</option>
                        <option value="الصحة الحيوانية">الصحة الحيوانية</option>
                        <option value="البيئة">البيئة</option>
                        <option value="متعدد التخصصات">متعدد التخصصات</option>
                      </select>
                    </div>

                    <!-- نوع العرض -->
                    <div>
                      <label for="choixCommunication" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                        نوع العرض <span class="text-red-500">*</span>
                      </label>
                      <select
                        id="choixCommunication"
                        name="choixCommunication"
                        class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                      >
                        <option value="">اختر النوع</option>
                        <option value="عرض شفهي">عرض شفهي</option>
                        <option value="ملصق">ملصق</option>
                      </select>
                    </div>
                  </div>

                  <!-- رفع ملف الملخص -->
                  <div>
                    <label for="resumeFile" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">
                      الملخص (ملف) <span class="text-red-500">*</span>
                    </label>
                    <div class="relative">
                      <input
                        type="file"
                        id="resumeFile"
                        name="resumeFile"
                        accept=".pdf,.doc,.docx"
                        class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-semibold file:bg-brand-primary file:text-white hover:file:bg-brand-primaryDark"
                      />
                    </div>
                    <p class="mt-2 text-sm text-slate-500 dark:text-slate-400">
                      الصيغ المقبولة: PDF, DOC, DOCX (الحد الأقصى 5 ميغابايت)
                    </p>
                  </div>
                </div>
              </div>

              <!-- أزرار التنقل -->
              <div class="flex justify-between pt-6">
                <button
                  type="button"
                  id="prev-step"
                  class="px-8 py-3 border border-slate-300 dark:border-slate-600 text-slate-700 dark:text-slate-300 font-semibold rounded-lg hover:bg-slate-50 dark:hover:bg-slate-700 transition-colors"
                >
                  السابق →
                </button>
                <button
                  type="submit"
                  id="submit-form"
                  class="px-8 py-3 bg-green-600 hover:bg-green-700 text-white font-semibold rounded-lg transition-all duration-300 hover:scale-105 hover:shadow-lg focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2 dark:focus:ring-offset-slate-800"
                >
                  إرسال التسجيل
                </button>
              </div>
            </div>
          </form>

          <!-- Status Messages -->
          <div id="status-message" class="hidden mb-6">
            <div id="status-content" class="p-4 rounded-lg text-center font-medium">
              <!-- Dynamic content will be inserted here -->
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
  
  <Footer locale={locale} />
</BaseLayout>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const form = document.getElementById('registration-form');
  const step1 = document.getElementById('step1');
  const step2 = document.getElementById('step2');
  const nextBtn = document.getElementById('next-step');
  const prevBtn = document.getElementById('prev-step');
  const step1Indicator = document.getElementById('step1-indicator');
  const step2Indicator = document.getElementById('step2-indicator');
  const participationRadios = document.querySelectorAll('input[name="typeParticipation"]');
  const presenterFields = document.getElementById('presenter-fields');
  const successMessage = document.getElementById('status-message');
  const loadingMessage = document.getElementById('status-message');

  // Google Apps Script endpoint
  const SCRIPT_URL = 'https://script.google.com/macros/s/XXXXXXXX/exec';

  // Step navigation
  nextBtn.addEventListener('click', function() {
    if (validateStep1()) {
      showStep2();
    }
  });

  prevBtn.addEventListener('click', function() {
    showStep1();
  });

  // Participation type change
  participationRadios.forEach(radio => {
    radio.addEventListener('change', function() {
      if (this.value === 'مقدم عرض') {
        presenterFields.classList.remove('hidden');
        // Make presenter fields required
        const presenterInputs = presenterFields.querySelectorAll('input[required], select[required]');
        presenterInputs.forEach(input => input.setAttribute('required', 'required'));
      } else {
        presenterFields.classList.add('hidden');
        // Remove required attribute from presenter fields
        const presenterInputs = presenterFields.querySelectorAll('input, select');
        presenterInputs.forEach(input => input.removeAttribute('required'));
      }
    });
  });

  // Form submission
  form.addEventListener('submit', async function(e) {
    e.preventDefault();
    
    if (!validateStep2()) {
      return;
    }

    // Show loading with Arabic text
    showMessage('loading', '⏳ جاري الإرسال...');

    try {
      const formData = collectFormData();
      
      const response = await fetch(SCRIPT_URL, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify(formData)
      });

      const result = await response.json();

      if (response.ok && result.result === 'success') {
        // Show success message in Arabic
        showMessage('success', '✅ تم تسجيل مشاركتك بنجاح.');
      } else {
        throw new Error('Submission failed');
      }
    } catch (error) {
      console.error('Error:', error);
      // Show error message in Arabic
      showMessage('error', '❌ حدث خطأ أثناء الإرسال. المرجو إعادة المحاولة.');
    }
  });

  function validateStep1() {
    const requiredFields = step1.querySelectorAll('input[required], select[required]');
    let isValid = true;

    requiredFields.forEach(field => {
      if (!field.value.trim()) {
        field.classList.add('border-red-500');
        isValid = false;
      } else {
        field.classList.remove('border-red-500');
      }
    });

    if (!isValid) {
      showMessage('error', '❌ يرجى ملء جميع الحقول المطلوبة.');
      return false;
    }

    return isValid;
  }

  function validateStep2() {
    const participationType = document.querySelector('input[name="typeParticipation"]:checked');
    
    if (!participationType) {
      showMessage('error', '❌ يرجى اختيار نوع مشاركتك.');
      return false;
    }

    if (participationType.value === 'مقدم عرض') {
      const presenterRequiredFields = presenterFields.querySelectorAll('input[required], select[required]');
      let isValid = true;

      presenterRequiredFields.forEach(field => {
        if (!field.value.trim()) {
          field.classList.add('border-red-500');
          isValid = false;
        } else {
          field.classList.remove('border-red-500');
        }
      });

      if (!isValid) {
        showMessage('error', '❌ يرجى ملء جميع الحقول المطلوبة لمقدمي العروض.');
        return false;
      }
    }

    return true;
  }

  function showStep1() {
    step1.classList.remove('hidden');
    step2.classList.add('hidden');
    step1Indicator.classList.remove('bg-slate-300', 'dark:bg-slate-600', 'text-slate-500');
    step1Indicator.classList.add('bg-brand-primary', 'text-white');
    step2Indicator.classList.remove('bg-brand-primary', 'text-white');
    step2Indicator.classList.add('bg-slate-300', 'dark:bg-slate-600', 'text-slate-500');
  }

  function showStep2() {
    step1.classList.add('hidden');
    step2.classList.remove('hidden');
    step2Indicator.classList.remove('bg-slate-300', 'dark:bg-slate-600', 'text-slate-500');
    step2Indicator.classList.add('bg-brand-primary', 'text-white');
  }

  function collectFormData() {
    const formData = new FormData(form);
    const data = {};

    // Collect basic form data
    for (let [key, value] of formData.entries()) {
      if (key !== 'resumeFile') {
        data[key] = value;
      }
    }

    // Handle file upload (for now, just store filename)
    const fileInput = document.getElementById('resumeFile');
    if (fileInput && fileInput.files[0]) {
      data.resumeFileURL = fileInput.files[0].name;
    } else {
      data.resumeFileURL = '';
    }

    // Set default values for optional fields
    data.civilite = data.civilite || '';
    data.telephone = data.telephone || '';
    data.departement = data.departement || '';
    data.titreCommunication = data.titreCommunication || '';
    data.auteurs = data.auteurs || '';
    data.auteurCorrespondant = data.auteurCorrespondant || '';
    data.thematique = data.thematique || '';
    data.choixCommunication = data.choixCommunication || '';

    return data;
  }

  function showMessage(type, message) {
    const statusMessage = document.getElementById('status-message');
    const statusContent = document.getElementById('status-content');
    
    // Reset classes
    statusContent.className = 'p-4 rounded-lg text-center font-medium';
    
    // Add type-specific classes
    if (type === 'success') {
      statusContent.classList.add('bg-green-500', 'text-white');
    } else if (type === 'error') {
      statusContent.classList.add('bg-red-500', 'text-white');
    } else if (type === 'loading') {
      statusContent.classList.add('bg-blue-500', 'text-white');
    }
    
    // Set message and show
    statusContent.textContent = message;
    statusMessage.classList.remove('hidden');
    
    // Hide form during loading/success
    if (type === 'loading' || type === 'success') {
      form.classList.add('hidden');
    } else {
      form.classList.remove('hidden');
    }
    
    // Auto-hide error messages after 5 seconds
    if (type === 'error') {
      setTimeout(() => {
        statusMessage.classList.add('hidden');
      }, 5000);
    }
  }
});
</script>