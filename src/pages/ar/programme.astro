---
// src/pages/ar/programme.astro
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { useTranslations } from '../../i18n/ui';
import { link } from '../../utils/paths';

const locale = 'ar';
const t = useTranslations(locale);

// --- DATA ---
const day1 = [
  { time: '09:00 – 10:00', session: 'استقبال المشاركين والتسجيل', speakers: 'فريق التنظيم', type: 'event' },
  { time: '10:00 – 11:00', session: 'الجلسة الافتتاحية / المسؤولون', speakers: 'أ.د فاسي فهري، وزير الفلاحة، مدير IAV، نائب رئيس UH2', type: 'plenary' },
  { time: '11:00 – 11:45', session: 'المحاضرة العامة الافتتاحية\nالصحة الواحدة 2030: نحو حوكمة متكاملة', speakers: 'المحاضرة: أ.د فاسي فهري\nإدارة الجلسة: أ.د طانان', type: 'plenary' },
  { time: '11:45 – 12:15', session: 'استراحة قهوة', speakers: '', type: 'break' },
  { time: '12:15 – 13:00', session: 'الجلسة 1: الواجهات البيولوجية والطوارئ الصحية\nالأمن الحيوي والصحة الواحدة', speakers: 'المحاضرة: أ.د بلقاضي\nإدارة الجلسة: أ.د مدراوي و أ.د نوري', type: 'session' },
  { time: '13:00 – 14:00', session: 'العروض الشفهية / الجلسة 1', speakers: 'أ.د نوري و أ.د مدراوي', type: 'oral' },
  { time: '14:00 – 14:30', session: 'استراحة غداء', speakers: '', type: 'break' },
  { time: '14:30 – 15:15', session: 'الجلسة 2: الأنظمة والأمن الغذائي', speakers: 'المحاضر: أ.د سارة ريمون\nإدارة الجلسة: أ.د رواس و د. موجبير', type: 'session' },
  { time: '15:15 – 16:15', session: 'العروض الشفهية / الجلسة 2', speakers: 'أ.د رواس و د. موجبير', type: 'oral' },
  { time: '16:15 – 16:30', session: 'استراحة قهوة', speakers: '', type: 'break' },
  { time: '16:30 – 17:30', session: 'ورشة العمل 1', speakers: 'AMBS', type: 'workshop' },
  { time: '17:30 – 18:30', session: 'ورشة العمل 2', speakers: 'أ.د كسراوي عزيز و د. الموسطي', type: 'workshop' },
];

const day2 = [
  { time: '09:00 – 09:45', session: 'الجلسة 3: البيئة والصحة العالمية\nالإجهاد البيئي والاضطرابات التناسلية', speakers: 'المحاضرة: أ.د جولي غارنييه\nإدارة الجلسة: أ.د مذكور و أ.د لعكيلي', type: 'session' },
  { time: '09:45 – 10:45', session: 'العروض الشفهية / الجلسة 3', speakers: 'إدارة الجلسة: أ.د مذكور و أ.د لعكيلي', type: 'oral' },
  { time: '10:45 – 11:00', session: 'استراحة قهوة', speakers: '', type: 'break' },
  { time: '11:00 – 11:45', session: 'الجلسة 4: الكيمياء والأمن الحيوي', speakers: 'المحاضر: أ.د ميشو\nإدارة الجلسة: أ.د طانان و د. الموسطي', type: 'session' },
  { time: '11:45 – 12:45', session: 'العروض الشفهية / الجلسة 4', speakers: 'إدارة الجلسة: أ.د طانان و د. الموسطي', type: 'oral' },
  { time: '12:45 – 13:30', session: 'الجلسة 5: التقنيات الناشئة والذكاء الاصطناعي', speakers: 'المحاضر: أ.د جاكوب زينستاغ\nإدارة الجلسة: أ.د نوري و د. الموسطي', type: 'session' },
  { time: '13:30 – 14:30', session: 'العروض الشفهية / الجلسة 5', speakers: 'إدارة الجلسة: أ.د نوري و د. الموسطي', type: 'oral' },
  { time: '14:30 – 15:00', session: 'استراحة غداء', speakers: '', type: 'break' },
  { time: '15:00 – 15:45', session: 'الجلسة 6: الاستدامة والمرونة', speakers: 'المحاضر: أ.د جيوفاني غيلميتي\nإدارة الجلسة: أ.د مكامل و أ.د منوسي', type: 'session' },
  { time: '15:45 – 16:45', session: 'العروض الشفهية / الجلسة 6', speakers: 'أ.د مكامل و أ.د منوسي', type: 'oral' },
  { time: '16:45 – 17:00', session: 'استراحة قهوة', speakers: '', type: 'break' },
  { time: '17:00 – 18:00', session: 'ورشة العمل 3', speakers: 'أ.د شكوري و أ.د مكامل', type: 'workshop' },
  { time: '18:00 – 19:00', session: 'ورشة العمل 4', speakers: 'أ.د مذكور و أ.د نوري', type: 'workshop' },
  { time: '19:00 – 19:30', session: 'جلسة الاختتام', speakers: 'لجنة التنظيم', type: 'plenary' },
];

// Styles Helper
function getStyles(type: string) {
  switch(type) {
    case 'break': return {
      container: 'border-r-4 border-amber-400 bg-amber-50/50 dark:bg-amber-900/10',
      icon: '☕',
      text: 'text-amber-800 dark:text-amber-200'
    };
    case 'plenary': return {
      container: 'border-r-4 border-brand-primary bg-brand-primary/5 dark:bg-brand-primary/10 shadow-sm',
      icon: '🎤',
      text: 'text-brand-primary dark:text-brand-accent'
    };
    case 'session': return {
      container: 'border-r-4 border-teal-500 bg-white dark:bg-slate-800 shadow-md hover:shadow-lg',
      icon: '🔬',
      text: 'text-teal-700 dark:text-teal-300'
    };
    case 'workshop': return {
      container: 'border-r-4 border-purple-500 bg-white dark:bg-slate-800 shadow-md hover:shadow-lg',
      icon: '🛠️',
      text: 'text-purple-700 dark:text-purple-300'
    };
    case 'oral': return {
      container: 'border-r-4 border-slate-300 bg-slate-50 dark:bg-slate-800/50',
      icon: '📑',
      text: 'text-slate-600 dark:text-slate-400'
    };
    default: return {
      container: 'border-r-4 border-gray-300',
      icon: '📅',
      text: 'text-gray-600'
    };
  }
}
---

<BaseLayout
  title="البرنامج العلمي - CIRIV'26"
  description="اكتشف البرنامج الكامل لمؤتمر CIRIV'26"
  locale={locale}
>
  <Header locale={locale} currentPath={Astro.url.pathname} />
  
  <main dir="rtl" class="min-h-screen bg-slate-50 dark:bg-slate-900 pb-20">
    
    <div class="relative bg-gradient-to-bl from-brand-primary to-teal-700 dark:from-slate-900 dark:to-slate-800 pt-24 pb-32 overflow-hidden">
      <div class="absolute top-0 right-0 w-64 h-64 bg-white/5 rounded-full translate-x-1/2 -translate-y-1/2 blur-3xl"></div>
      <div class="absolute bottom-0 left-0 w-96 h-96 bg-brand-accent/10 rounded-full -translate-x-1/3 translate-y-1/3 blur-3xl"></div>
      
      <div class="container mx-auto px-4 relative z-10 text-center">
        <span class="inline-block animate-fade-in-down px-4 py-1.5 rounded-full bg-white/10 backdrop-blur-md border border-white/20 text-white text-sm font-medium mb-6">
          7 – 8 أبريل 2026 • الرباط، المغرب
        </span>
        <h1 class="text-4xl md:text-6xl font-bold text-white mb-6 animate-fade-in-up">
          البرنامج العلمي
        </h1>
        <p class="text-lg text-slate-100 max-w-2xl mx-auto opacity-90 animate-fade-in-up delay-100">
          يومان من الابتكار والتبادل واستراتيجيات "الصحة الواحدة" مع محاضرين وورش عمل عالمية المستوى.
        </p>
      </div>
    </div>

    <div class="container mx-auto px-4 -mt-20 relative z-20">
      
      <div class="flex justify-center mb-12">
        <div class="bg-white dark:bg-slate-800 p-1.5 rounded-2xl shadow-xl flex gap-2">
          <button 
            id="btn-day1"
            class="tab-btn px-8 py-3 rounded-xl text-sm md:text-base font-bold transition-all duration-300 bg-brand-primary text-white shadow-md"
            onclick="switchTab('day1')"
          >
            اليوم 1 (7 أبريل)
          </button>
          <button 
            id="btn-day2"
            class="tab-btn px-8 py-3 rounded-xl text-sm md:text-base font-medium transition-all duration-300 text-slate-600 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-700"
            onclick="switchTab('day2')"
          >
            اليوم 2 (8 أبريل)
          </button>
        </div>
      </div>

      <div class="max-w-4xl mx-auto">
        
        <div id="content-day1" class="space-y-6 schedule-container">
          {day1.map((item, index) => {
            const style = getStyles(item.type);
            return (
              <div 
                class={`group relative flex flex-col md:flex-row-reverse gap-4 md:gap-8 p-6 rounded-xl transition-all duration-300 hover:-translate-y-1 ${style.container}`}
                style={`animation-delay: ${index * 50}ms`} 
              >
                <div class="md:w-32 flex-shrink-0 flex md:flex-col items-center md:items-end gap-2">
                  <span class="text-sm font-bold text-slate-900 dark:text-white bg-slate-100 dark:bg-slate-700 px-3 py-1 rounded-md">
                    {item.time}
                  </span>
                  <div class="hidden md:block h-full w-px bg-slate-200 dark:bg-slate-700 my-2 mx-auto md:mx-0 md:mr-4"></div>
                </div>

                <div class="flex-grow text-right">
                  <div class="flex items-start justify-between gap-4 mb-2 flex-row-reverse">
                    <h3 class="text-lg md:text-xl font-bold text-slate-900 dark:text-white leading-tight">
                      {item.session}
                    </h3>
                    <span class="text-2xl">{style.icon}</span>
                  </div>
                  
                  {item.speakers && (
                    <div class={`mt-3 text-sm md:text-base flex items-start gap-2 flex-row-reverse ${style.text}`}>
                      <svg class="w-5 h-5 mt-0.5 flex-shrink-0 opacity-70" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg>
                      <span class="whitespace-pre-line font-medium opacity-90">{item.speakers}</span>
                    </div>
                  )}
                  
                  {item.type === 'break' && (
                     <p class="text-sm text-amber-600/70 italic mt-1">تواصل ومرطبات</p>
                  )}
                </div>
              </div>
            );
          })}
        </div>

        <div id="content-day2" class="space-y-6 hidden schedule-container">
          {day2.map((item, index) => {
            const style = getStyles(item.type);
            return (
              <div 
                class={`group relative flex flex-col md:flex-row-reverse gap-4 md:gap-8 p-6 rounded-xl transition-all duration-300 hover:-translate-y-1 ${style.container}`}
                style={`animation-delay: ${index * 50}ms`} 
              >
                <div class="md:w-32 flex-shrink-0 flex md:flex-col items-center md:items-end gap-2">
                  <span class="text-sm font-bold text-slate-900 dark:text-white bg-slate-100 dark:bg-slate-700 px-3 py-1 rounded-md">
                    {item.time}
                  </span>
                  <div class="hidden md:block h-full w-px bg-slate-200 dark:bg-slate-700 my-2 mx-auto md:mx-0 md:mr-4"></div>
                </div>

                <div class="flex-grow text-right">
                  <div class="flex items-start justify-between gap-4 mb-2 flex-row-reverse">
                    <h3 class="text-lg md:text-xl font-bold text-slate-900 dark:text-white leading-tight">
                      {item.session}
                    </h3>
                    <span class="text-2xl">{style.icon}</span>
                  </div>
                  
                  {item.speakers && (
                    <div class={`mt-3 text-sm md:text-base flex items-start gap-2 flex-row-reverse ${style.text}`}>
                      <svg class="w-5 h-5 mt-0.5 flex-shrink-0 opacity-70" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg>
                      <span class="whitespace-pre-line font-medium opacity-90">{item.speakers}</span>
                    </div>
                  )}
                </div>
              </div>
            );
          })}
        </div>

      </div>
    </div>
  </main>

  <Footer locale={locale} />

  <script is:inline>
    function switchTab(day) {
      const btn1 = document.getElementById('btn-day1');
      const btn2 = document.getElementById('btn-day2');
      const content1 = document.getElementById('content-day1');
      const content2 = document.getElementById('content-day2');

      const activeClass = ['bg-brand-primary', 'text-white', 'shadow-md'];
      const inactiveClass = ['text-slate-600', 'dark:text-slate-300', 'hover:bg-slate-100', 'dark:hover:bg-slate-700'];

      if (day === 'day1') {
        btn1.classList.add(...activeClass);
        btn1.classList.remove(...inactiveClass);
        btn2.classList.remove(...activeClass);
        btn2.classList.add(...inactiveClass);
        content2.classList.add('hidden');
        content1.classList.remove('hidden');
        triggerAnimation(content1);
      } else {
        btn2.classList.add(...activeClass);
        btn2.classList.remove(...inactiveClass);
        btn1.classList.remove(...activeClass);
        btn1.classList.add(...inactiveClass);
        content1.classList.add('hidden');
        content2.classList.remove('hidden');
        triggerAnimation(content2);
      }
    }

    function triggerAnimation(element) {
      element.classList.remove('schedule-container');
      void element.offsetWidth;
      element.classList.add('schedule-container');
    }
  </script>

  <style>
    .schedule-container > div {
      opacity: 0;
      animation: fadeInUp 0.6s ease-out forwards;
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .delay-100 { animation-delay: 100ms; }
    
    .animate-fade-in-up {
      animation: fadeInUp 0.8s ease-out forwards;
    }
    
    .animate-fade-in-down {
      animation: fadeInDown 0.8s ease-out forwards;
    }

    @keyframes fadeInDown {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</BaseLayout>
