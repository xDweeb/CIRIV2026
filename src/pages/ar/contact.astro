---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { useTranslations } from '../../i18n/ui';

const locale = 'ar';
const t = useTranslations(locale);
---

<BaseLayout
  title="اتصل بنا - CIRIV 2026"
  description="اتصل بفريق تنظيم مؤتمر CIRIV 2026 لأي سؤال أو معلومة"
  locale={locale}
>
  <Header locale={locale} currentPath={Astro.url.pathname} />
  
  <main class="min-h-screen bg-gradient-to-br from-slate-50 to-teal-50 dark:from-surface-dark dark:to-slate-800 py-12">
    <div class="container mx-auto px-4">
      <!-- Header -->
      <div class="text-center mb-16">
        <h1 class="text-4xl lg:text-5xl font-bold text-slate-900 dark:text-white mb-4">
          اتصل بنا
        </h1>
        <p class="text-xl text-slate-600 dark:text-slate-300 max-w-3xl mx-auto">
          فريقنا متاح للإجابة على جميع أسئلتك حول CIRIV 2026
        </p>
      </div>

      <div class="max-w-6xl mx-auto">
        <div class="grid lg:grid-cols-3 gap-8">
          <!-- Contact Information -->
          <div class="lg:col-span-1 lg:order-last">
            <div class="bg-white dark:bg-slate-800 rounded-2xl shadow-xl p-8 h-full">
              <h2 class="text-2xl font-bold text-slate-900 dark:text-white mb-8 text-right">
                معلومات الاتصال
              </h2>
              
              <!-- Email -->
              <div class="flex items-start gap-4 mb-8 flex-row-reverse">
                <div class="w-12 h-12 bg-brand-primary/10 rounded-lg flex items-center justify-center flex-shrink-0">
                  <svg class="w-6 h-6 text-brand-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                  </svg>
                </div>
                <div class="text-right">
                  <h3 class="font-semibold text-slate-900 dark:text-white mb-1">البريد الإلكتروني</h3>
                  <a 
                    href="mailto:info@ciriv.org" 
                    class="text-brand-primary hover:text-brand-primaryDark transition-colors"
                  >
                    info@ciriv.org
                  </a>
                </div>
              </div>

              <!-- Address -->
              <div class="flex items-start gap-4 mb-8 flex-row-reverse">
                <div class="w-12 h-12 bg-brand-primary/10 rounded-lg flex items-center justify-center flex-shrink-0">
                  <svg class="w-6 h-6 text-brand-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                  </svg>
                </div>
                <div class="text-right">
                  <h3 class="font-semibold text-slate-900 dark:text-white mb-1">العنوان</h3>
                  <p class="text-slate-600 dark:text-slate-300">
                    معهد الحسن الثاني للزراعة والبيطرة<br />
                    مدينة العرفان، شارع علال الفاسي<br />
                    الرباط – المغرب
                  </p>
                  <a
                    href="https://maps.google.com/?q=Institut+Agronomique+et+Veterinaire+Hassan+II+Rabat"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="inline-flex items-center gap-1 mt-2 text-brand-primary hover:text-brand-primaryDark transition-colors text-sm"
                  >
                    عرض على الخريطة
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
                    </svg>
                  </a>
                </div>
              </div>

              <!-- Social Links -->
              <div>
                <h3 class="font-semibold text-slate-900 dark:text-white mb-4 text-right">تابعنا</h3>
                <div class="flex gap-4 justify-end">
                  <a
                    href="https://www.linkedin.com/company/ciriv.congres"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="w-12 h-12 bg-slate-100 dark:bg-slate-700 text-slate-600 dark:text-slate-300 border-2 border-slate-200 dark:border-slate-600 hover:border-blue-500 hover:text-blue-600 dark:hover:text-blue-400 transition-all duration-300 rounded-xl flex items-center justify-center"
                    aria-label="LinkedIn"
                  >
                    <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                      <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
                    </svg>
                  </a>
                  <a
                    href="https://www.instagram.com/ciriv.congres"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="w-12 h-12 bg-slate-100 dark:bg-slate-700 text-slate-600 dark:text-slate-300 border-2 border-slate-200 dark:border-slate-600 hover:border-pink-500 hover:text-pink-500 dark:hover:text-pink-400 transition-all duration-300 rounded-xl flex items-center justify-center"
                    aria-label="Instagram"
                  >
                    <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                      <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
                    </svg>
                  </a>
                </div>
              </div>
            </div>
          </div>

          <!-- Contact Form -->
          <div class="lg:col-span-2">
            <div class="bg-white dark:bg-slate-800 rounded-2xl shadow-xl p-8">
              <h2 class="text-2xl font-bold text-slate-900 dark:text-white mb-8 text-right">
                أرسل لنا رسالة
              </h2>

              <form id="contact-form" class="space-y-6" action="#" method="POST">
                <div class="grid md:grid-cols-2 gap-6">
                  <!-- Name -->
                  <div>
                    <label for="contact-name" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2 text-right">
                      الاسم الكامل <span class="text-red-500">*</span>
                    </label>
                    <input
                      type="text"
                      id="contact-name"
                      name="name"
                      required
                      class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors text-right"
                      placeholder="اسمك الكامل"
                    />
                  </div>

                  <!-- Email -->
                  <div>
                    <label for="contact-email" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2 text-right">
                      البريد الإلكتروني <span class="text-red-500">*</span>
                    </label>
                    <input
                      type="email"
                      id="contact-email"
                      name="email"
                      required
                      class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors"
                      placeholder="your@email.com"
                    />
                  </div>
                </div>

                <!-- Subject -->
                <div>
                  <label for="contact-subject" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2 text-right">
                    الموضوع <span class="text-red-500">*</span>
                  </label>
                  <select
                    id="contact-subject"
                    name="subject"
                    required
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors text-right"
                  >
                    <option value="">اختر موضوعاً</option>
                    <option value="registration">أسئلة حول التسجيل</option>
                    <option value="program">معلومات البرنامج</option>
                    <option value="logistics">اللوجستيات والإقامة</option>
                    <option value="partnership">فرص الشراكة</option>
                    <option value="press">استفسارات صحفية</option>
                    <option value="other">أخرى</option>
                  </select>
                </div>

                <!-- Message -->
                <div>
                  <label for="contact-message" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-2 text-right">
                    الرسالة <span class="text-red-500">*</span>
                  </label>
                  <textarea
                    id="contact-message"
                    name="message"
                    rows="6"
                    required
                    class="w-full px-4 py-3 border border-slate-300 dark:border-slate-600 rounded-lg focus:ring-2 focus:ring-brand-accent focus:border-transparent dark:bg-slate-700 dark:text-white transition-colors resize-vertical text-right"
                    placeholder="رسالتك..."
                  ></textarea>
                </div>

                <!-- Submit Button -->
                <button
                  type="submit"
                  id="submit-btn"
                  class="w-full px-6 py-4 bg-brand-primary hover:bg-brand-primaryDark text-white font-semibold rounded-lg transition-all duration-300 hover:scale-105 hover:shadow-lg hover:shadow-brand-primary/25 focus:outline-none focus:ring-2 focus:ring-brand-accent focus:ring-offset-2 dark:focus:ring-offset-slate-800 disabled:opacity-75 disabled:cursor-not-allowed disabled:hover:scale-100"
                >
                  <span id="btn-text">إرسال الرسالة</span>
                  <div id="btn-loading" class="hidden flex items-center justify-center">
                    <svg class="animate-spin -ml-1 mr-3 h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                      <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                      <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                    </svg>
                    جارٍ الإرسال...
                  </div>
                </button>

                <p class="text-sm text-slate-500 dark:text-slate-400 text-center">
                  <span class="text-red-500">*</span> الحقول المطلوبة
                </p>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
  
  <Footer locale={locale} />
</BaseLayout>

<!-- EmailJS -->
<script src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"></script>

<script>
  // Initialize EmailJS when script loads
  (function() {
    // Wait for EmailJS to load
    function initEmailJS() {
      if (typeof emailjs !== 'undefined') {
        emailjs.init("nxVtz6ayXRhDikg7Z");
        console.log('EmailJS initialized successfully');
      } else {
        setTimeout(initEmailJS, 100);
      }
    }
    initEmailJS();
  })();

  // Form submission handler
  document.addEventListener('DOMContentLoaded', function() {
    const form = document.getElementById('contact-form');
    const submitBtn = document.getElementById('submit-btn');
    const btnText = document.getElementById('btn-text');
    const btnLoading = document.getElementById('btn-loading');

    console.log('Form elements found:', { form, submitBtn, btnText, btnLoading });

    if (form && submitBtn && btnText && btnLoading) {
      form.addEventListener('submit', function(e) {
        e.preventDefault();
        e.stopPropagation();
        
        console.log('Form submission intercepted');
        
        // Check if EmailJS is loaded
        if (typeof emailjs === 'undefined') {
          showAlert('خطأ في تحميل خدمة الإرسال. يرجى إعادة تحميل الصفحة.', 'error');
          return;
        }

        // Show loading state
        submitBtn.disabled = true;
        btnText.classList.add('hidden');
        btnLoading.classList.remove('hidden');
        submitBtn.classList.add('opacity-75', 'cursor-not-allowed');

        console.log('Sending email via EmailJS...');

        // Send email using EmailJS
        emailjs.sendForm('service_48d81jb', 'template_jqegk6d', form)
          .then(function(response) {
            console.log('Email sent successfully:', response);
            showAlert('تم إرسال الرسالة بنجاح!', 'success');
            form.reset();
          })
          .catch(function(error) {
            console.error('EmailJS Error:', error);
            showAlert('فشل في إرسال الرسالة. يرجى المحاولة مرة أخرى.', 'error');
          })
          .finally(function() {
            // Reset button state
            setTimeout(() => {
              submitBtn.disabled = false;
              btnText.classList.remove('hidden');
              btnLoading.classList.add('hidden');
              submitBtn.classList.remove('opacity-75', 'cursor-not-allowed');
            }, 1000);
          });
      });
    } else {
      console.error('Form elements not found properly');
    }

    // Custom alert function with animations
    function showAlert(message, type) {
      // Remove existing alerts
      const existingAlert = document.querySelector('.custom-alert');
      if (existingAlert) {
        existingAlert.remove();
      }

      // Create alert element
      const alert = document.createElement('div');
      alert.className = `custom-alert fixed top-4 right-4 left-4 mx-auto max-w-md z-50 p-4 rounded-lg shadow-lg transform translate-y-[-100px] opacity-0 transition-all duration-500 ease-out`;
      
      if (type === 'success') {
        alert.className += ' bg-green-500 text-white';
        alert.innerHTML = `
          <div class="flex items-center gap-3">
            <svg class="w-6 h-6 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
            <span class="font-medium">${message}</span>
          </div>
        `;
      } else {
        alert.className += ' bg-red-500 text-white';
        alert.innerHTML = `
          <div class="flex items-center gap-3">
            <svg class="w-6 h-6 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
            <span class="font-medium">${message}</span>
          </div>
        `;
      }

      // Add to body
      document.body.appendChild(alert);

      // Animate in
      setTimeout(() => {
        alert.classList.remove('translate-y-[-100px]', 'opacity-0');
        alert.classList.add('translate-y-0', 'opacity-100');
      }, 100);

      // Add success animation for button
      if (type === 'success' && submitBtn) {
        submitBtn.classList.add('bg-green-500');
        setTimeout(() => {
          submitBtn.classList.remove('bg-green-500');
        }, 2000);
      }

      // Auto remove after 5 seconds
      setTimeout(() => {
        alert.classList.add('translate-y-[-100px]', 'opacity-0');
        setTimeout(() => {
          alert.remove();
        }, 500);
      }, 5000);

      // Click to dismiss
      alert.addEventListener('click', () => {
        alert.classList.add('translate-y-[-100px]', 'opacity-0');
        setTimeout(() => {
          alert.remove();
        }, 500);
      });
    }
  });
</script>

<style>
  /* Loading animation for button */
  @keyframes pulse {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: 0.5;
    }
  }

  #submit-btn:disabled {
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }

  /* Alert animations */
  .custom-alert {
    backdrop-filter: blur(8px);
  }

  /* Success button animation */
  .bg-green-500 {
    background-color: #10b981 !important;
    transition: background-color 0.3s ease;
  }
</style>